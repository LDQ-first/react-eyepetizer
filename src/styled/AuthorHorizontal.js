import styled from 'styled-components'
import {mediaQuery} from './index.js'
import {eyeApi} from '../api/api.js' 

const AuthorHorizontalDiv = styled.div`
     .authors {
        .author {
            margin: 20px 10px;
        }
    }
`

export default AuthorHorizontalDiv